#include"list.h"
#include<stdlib.h>

int main()
{
	/*List* test_list = NULL;
	ListIterator* test_iterator = NULL;

	int test_data[5];
	int test_int;
	int lenght;

	for (int i = 0; i < 5; i++)
	{
		test_data[i] = 2343242 % (i + 54);
	}
	
	create(&test_list, &(test_data[0]));
	add(test_list, &(test_data[1]));
	add(test_list, test_data + 2);
	add_to_end(test_list, test_data + 3);
	
	test_int = *(int *)(get(test_list, 2));

	test_iterator = get_iterator(test_list);

	lenght = get_length(test_list);

	while (has_next(test_iterator))
	{
		move_next(test_iterator);
	}
	test_int = *(int*)(get_value(test_iterator));

	remove(test_list, 4);
	remove(test_list, 3);
	remove(test_list, 2);
	remove(test_list, 1);
	remove(test_list, 0);*/

	return 0;
}